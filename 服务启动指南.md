# TestHub 平台服务启动指南

本文档说明如何启动 TestHub 平台的前后端服务。

## 前提条件

- Docker Desktop 已启动（MySQL、Redis 等容器服务已运行）
- Python 3.12+ 已安装
- Node.js 已安装

## 一、后端服务启动 (Django)

### 启动目录
```
D:\AI\syswin-testhub\testhub-platform-src
```

### 启动命令
```bash
python manage.py runserver
```

### 访问地址
- 后端服务: http://127.0.0.1:8000/
- API 文档 (Swagger): http://127.0.0.1:8000/api/docs/
- API 文档 (ReDoc): http://127.0.0.1:8000/api/redoc/

### 可选：使用虚拟环境启动
```bash
# 激活虚拟环境 (如果有)
E:\python_venv\testhub\Scripts\activate.bat

# 启动服务
python manage.py runserver
```

### 停止服务
按 `Ctrl + C` 或 `Ctrl + Break`

---

## 二、前端服务启动 (Vue 3 + Vite)

### 启动目录
```
D:\AI\syswin-testhub\testhub-platform-src\frontend
```

### 启动命令
```bash
cd frontend
npm run dev
```

或者在 frontend 目录下直接执行：
```bash
npm run dev
```

### 访问地址
- 本地访问: http://localhost:3000/
- 局域网访问: http://192.168.10.136:3000/ (根据实际网络IP)

### 停止服务
按 `Ctrl + C`

---

## 三、完整启动流程

### 方式一：分别启动（推荐用于开发）

**终端 1 - 启动后端**
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
python manage.py runserver
```

**终端 2 - 启动前端**
```bash
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev
```

### 方式二：后台启动

可以使用项目中的启动脚本（如果有）或者创建批处理文件。

---

## 四、验证服务状态

### 检查后端服务
访问 http://127.0.0.1:8000/api/docs/ 查看 API 文档

### 检查前端服务
访问 http://localhost:3000/ 应该能看到登录页面

### 检查 Docker 容器
```bash
docker ps
```
确保 MySQL、Redis 等容器正在运行

---

## 五、常见问题

### 后端启动失败
1. 检查数据库连接配置（.env 文件）
2. 确认 Docker 容器服务已启动
3. 检查端口 8000 是否被占用

### 前端启动失败
1. 检查是否已安装依赖: `npm install`
2. 检查端口 3000 是否被占用
3. 清除缓存: `npm run clean` (如果有)

### 数据库迁移
如果是首次启动或模型有更新：
```bash
python manage.py makemigrations
python manage.py migrate
```

---

## 六、其他常用命令

### 后端
```bash
# 创建超级用户
python manage.py createsuperuser

# 运行所有定时任务
python manage.py run_all_scheduled_tasks

# 初始化 UI 自动化定位策略
python manage.py init_locator_strategies

# 下载 WebDriver
python manage.py download_webdrivers
```

### 前端
```bash
# 安装依赖
npm install

# 生产构建
npm run build

# 代码检查
npm run lint
```

---

**更新时间**: 2026-04-13
