# 服务重启指南

## 当前服务状态

### ✅ 已完成的准备工作
1. 数据库迁移已应用（0005_aidevelopmenttask_files_modified_and_more）
2. 前端代码已构建
3. 新字段验证通过：
   - `git_repository_url` - 存储Git仓库地址
   - `files_modified` - 存储修改的文件列表
   - `files_modified_list` - 序列化器自动解析JSON

### 📊 服务检查结果
- **Celery Worker**: ✅ 正在运行（PID: 27928, 24556）
- **Django Server**: ⚠️ 需要确认是否运行
- **前端**: ✅ 已构建到 `frontend/dist/`

---

## 重启服务步骤

### 方式一：完全重启（推荐）

#### 1. 停止所有服务

```bash
# 停止Django服务（如果在运行）
# 在运行Django的终端按 Ctrl+C

# 停止Celery Worker
taskkill /F /IM celery.exe

# 停止Celery Beat（如果在运行）
# 在运行Beat的终端按 Ctrl+C
```

#### 2. 重新启动服务

**终端1 - Django服务器**:
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
python manage.py runserver
```

**终端2 - Celery Worker**:
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
celery -A backend worker -l info
```

**终端3 - Celery Beat（可选，用于定时任务）**:
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
celery -A backend beat -l info
```

**终端4 - 前端开发服务器（开发环境）**:
```bash
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev
```

---

### 方式二：热重载（仅适用于Django）

如果Django支持自动重载，只需保存修改的Python文件即可。但为确保万无一失，建议完全重启。

---

## 验证服务运行

### 1. 检查Django
```bash
# 访问API文档
curl http://localhost:8000/api/docs/

# 或在浏览器打开
http://localhost:8000/api/docs/
```

### 2. 检查Celery Worker
查看终端输出，应该显示：
```
[tasks]
  . apps.ai_development.tasks.execute_ai_coding
  ...
```

### 3. 检查前端
```bash
# 开发环境
http://localhost:5173

# 或使用Django静态文件服务
http://localhost:8000
```

---

## 功能测试清单

### 测试1: 查看现有任务
1. 打开浏览器访问 `http://localhost:5173`（或对应端口）
2. 登录系统
3. 导航到 **AI开发 -> AI开发任务**
4. 查看任务列表
5. 点击任意任务的"详情"按钮
6. 确认可以看到任务详细信息

### 测试2: 查看任务日志
1. 在任务列表中，点击"日志"按钮
2. 切换到"执行日志"标签
3. 切换到"AI对话日志"标签
4. 确认两个标签页的日志都能正常显示

### 测试3: 创建新任务（验证新功能）
1. 确保有AI开发配置（AI开发 -> 开发配置）
2. 确保有需求文档（需求分析 -> 需求列表）
3. 创建新的AI开发任务：
   - 选择需求
   - 选择开发配置
   - 提交任务

4. 等待任务完成（可能需要几分钟到十几分钟）

5. 任务完成后，查看详情，确认显示：
   - ✅ Git仓库地址（带链接，可点击）
   - ✅ 开发分支名称
   - ✅ Commit Hash（可复制）
   - ✅ Commit Message（完整消息）
   - ✅ 修改的文件列表（以标签形式显示）
   - ✅ 服务URL（如果配置了启动命令）

### 测试4: 服务访问
1. 如果任务详情中显示了服务URL
2. 点击服务URL链接
3. 确认服务可以在浏览器中正常访问
4. 如果无法访问，参考《AI开发服务访问问题排查指南.md》

---

## 常见问题

### Q1: Django启动报错
**检查**:
- 确认数据库连接正常
- 确认所有依赖已安装 `pip install -r requirements.txt`
- 查看错误日志详细信息

### Q2: Celery无法连接
**检查**:
- 确认Redis/RabbitMQ等消息队列服务正在运行
- 查看 `backend/settings.py` 中的 `CELERY_BROKER_URL` 配置

### Q3: 前端页面显示异常
**解决**:
```bash
cd frontend
npm run build
```
重新构建前端代码

### Q4: 新字段不显示
**检查**:
- 确认数据库迁移已应用: `python manage.py showmigrations ai_development`
- 确认前端已重新构建
- 清除浏览器缓存并刷新页面（Ctrl+Shift+R）

---

## 快速诊断命令

```bash
# 检查端口占用
netstat -ano | findstr :8000  # Django
netstat -ano | findstr :5173  # 前端开发服务器

# 检查进程
tasklist | findstr python
tasklist | findstr celery
tasklist | findstr node

# 检查数据库迁移
python manage.py showmigrations ai_development

# 测试数据库连接
python manage.py dbshell

# 检查Django配置
python manage.py check
```

---

## 回滚指南（如遇问题）

如果更新后出现严重问题，可以回滚数据库迁移：

```bash
# 回滚到上一个迁移
python manage.py migrate ai_development 0004

# 查看当前状态
python manage.py showmigrations ai_development
```

⚠️ **注意**: 回滚会删除新添加的字段数据，请谨慎操作！

---

## 下一步

所有检查通过后，可以开始使用新功能：

1. 创建新的AI开发任务
2. 监控任务执行进度
3. 查看详细的Git提交信息
4. 追踪修改的文件
5. 访问部署的服务

如遇到问题，请参考：
- 《AI开发功能问题修复报告.md》
- 《AI开发服务访问问题排查指南.md》
