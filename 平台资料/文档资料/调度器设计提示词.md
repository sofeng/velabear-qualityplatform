本平台致力于借用AI能力实现整个研发全链路自动化：1-需求生成测试用例、2-根据需求调用AI编码、3-执行自动化测试用例、4-根据测试执行结果生成缺陷单、5-根据缺陷单调用AI修复缺陷、6-再执行自动化测试用例进行回归。当前平台只具备1、3的能力，现在我需要实现2的能力，需要在平台能配置：项目代码仓库git地址（支持根据配置的git账号与密码自动获取仓库所有项目的git地址并供选择）、git账号与密码、需要拉取git仓库的代码版本名称、项目所在服务器（支持选择：平台所在服务器、远程服务器，选择远程服务器时，支持配置IP地址与端口（默认22）及账号与密码）、项目代码路径、AI编码工具（Claude code（默认）、Codex CLI）、授权模式（完全授权（默认）、问答模式）、大模型（若【AI编码工具】选择Claude code，则可选值为：Sonnet、Opus、Haiku，默认值为Sonnet；若【AI编码工具】选择Codex CLI,则可选值为gpt-5.4 xhigh，默认值也是gpt-5.4 xhigh），期望在整个调起AI进行编码的过程具备如下能力：能在需求页面，调起【AI开发】功能，登录对应服务器（若【项目所在服务器】值为“远程服务器”），进入配置好的“项目代码路径“，通过命令进入所选【AI编码工具】对应的”授权模式“，并通过/model切换选择配置的”大模型“，让AI使用git账号密码到对应的代码git仓库地址拉取对应的”代码版本“的代码，根据需求进行开发，开发完后本地构建并调用本地的playwright/selenium（若已安装则直接调用，若为安装则自动安装）进行自测试，自测试通过后提交代码到git仓库并备注好需求编号和需求名称，并本地启动服务，然后将服务地址、账号密码返回需求列表。在整个调起AI进行编码的过程，每一步都需要及时回写状态到需求列表中。除了我以上想到的点，每个节点需要你想清楚每个细节，帮我补充和设计好，需要考虑每一步的可行性及安全性，对于调用及与Claude code和Codex CLI的对话的命令需要你整理出来配置在平台上，至于调起docker来进行开发还是直接在服务器本地进行开发也需要你给出合适的方案，至于没有playwright或者selenium是否需要让AI自动安装及相关依赖也需要你给出合适方案，平台多个用户同时发起多个需求开发如何调度也需要你给出合理的方案，等等更多细节和方案需要你帮我详细设计出来，让我选择哪套方案来进行开发。