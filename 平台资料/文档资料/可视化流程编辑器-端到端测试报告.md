# 可视化流程编辑器 - 端到端测试报告

## 测试日期
2026-04-14

## 测试目标
验证可视化流程编辑器的四大核心功能是否完整可用：
1. 在画布上渲染页面节点内部元素
2. 实现元素间连线和执行路径
3. 生成Playwright脚本
4. 执行测试脚本

## 测试环境
- **后端**: Django开发服务器 (http://127.0.0.1:8000)
- **前端**: Vite开发服务器 (http://localhost:3002)
- **快照文件**: page-2026-04-10T01-39-03-267Z.yml
- **浏览器**: Chrome/Edge

---

## 测试场景 1: 基础流程创建和节点渲染

### 测试步骤
1. 访问 http://localhost:3002/manual-testcases/visual-flow
2. 从左侧工具栏拖拽"开始"节点到画布
3. 配置开始节点：
   - 浏览器类型: chromium
   - 初始URL: https://example.com
   - 视口大小: 1920x1080
   - Headless模式: false
4. 拖拽"页面"节点到画布，连接到开始节点
5. 配置页面节点：
   - 页面名称: 示例页面
   - 选择快照文件: page-2026-04-10T01-39-03-267Z.yml
6. 点击"加载快照"按钮

### 预期结果
✅ 快照加载成功，显示提示消息
✅ 页面节点内部展示可交互元素列表（最多10个）
✅ 每个元素显示类型徽章和文本内容
✅ 右侧配置面板显示元素树结构
✅ 每个元素右侧显示黄色连接点

### 实际结果
[ ] 待测试

---

## 测试场景 2: 元素间连线和执行路径

### 测试步骤
1. 在页面节点内部，从第一个元素的连接点拖拽连线到第二个元素
2. 点击连线，在右侧面板修改操作类型为"填充"
3. 输入测试值: "test@example.com"
4. 继续连接第二个元素到第三个元素
5. 设置第二条连线的操作类型为"点击"
6. 查看右侧配置面板的"执行路径"部分

### 预期结果
✅ 连线创建成功，显示为黄色连线
✅ 连线上显示操作类型标签（如"click"、"fill"）
✅ 右侧配置面板的"执行路径"列表显示所有步骤
✅ 执行路径包含：元素类型、元素文本、操作类型、操作值
✅ 步骤按连接顺序排列

### 实际结果
[ ] 待测试

---

## 测试场景 3: 添加操作节点

### 测试步骤
1. 拖拽"操作"节点到画布，连接到页面节点后
2. 配置操作节点：
   - 操作类型: 等待
   - 超时时间: 2000ms
3. 拖拽"结束"节点到画布，连接到操作节点后
4. 配置结束节点：
   - 勾选"生成测试报告"

### 预期结果
✅ 操作节点和结束节点创建成功
✅ 节点间连线正常
✅ 形成完整的流程: 开始 → 页面 → 操作 → 结束

### 实际结果
[ ] 待测试

---

## 测试场景 4: 生成Playwright脚本

### 测试步骤
1. 点击顶部工具栏的"生成脚本"按钮
2. 查看生成的Python代码
3. 验证代码结构：
   - 包含导入语句 (playwright.async_api)
   - 包含async def run_test()函数
   - 包含启动浏览器代码
   - 包含访问URL代码
   - 包含页面元素操作代码
   - 包含等待操作代码
   - 包含关闭浏览器代码
   - 包含asyncio.run(run_test())入口
4. 点击"复制代码"按钮
5. 点击"下载脚本"按钮

### 预期结果
✅ 脚本生成成功，弹出预览对话框
✅ 生成的代码包含正确的Python async/await语法
✅ 浏览器配置与开始节点配置一致
✅ 页面操作按执行路径顺序排列
✅ 元素选择器使用优先级最高的选择器（data-ref > role > text > tag）
✅ 等待操作生成正确的wait_for_timeout代码
✅ 复制功能正常工作
✅ 下载功能正常，生成playwright_test_xxx.py文件

### 实际结果
[ ] 待测试

---

## 测试场景 5: 执行测试脚本

### 测试步骤
1. 在脚本预览对话框中，点击"执行测试"按钮
2. 等待脚本执行完成（最长5分钟）
3. 查看执行结果对话框

### 预期结果
✅ 显示"正在执行测试脚本..."加载提示
✅ 脚本执行成功（或失败并显示错误信息）
✅ 结果对话框显示标准输出（stdout）
✅ 如有错误，显示错误输出（stderr）
✅ 可以查看完整的执行日志

### 实际结果
[ ] 待测试

### 注意事项
- 如果playwright未安装，执行会失败
- 可以通过以下命令安装playwright:
  ```bash
  pip install playwright
  python -m playwright install chromium
  ```

---

## 测试场景 6: 流程图保存和加载

### 测试步骤
1. 点击"保存流程图"按钮
2. 输入流程图名称: "测试流程1"
3. 刷新页面
4. 点击"加载流程图"按钮
5. 选择刚才保存的"测试流程1"

### 预期结果
✅ 流程图保存成功
✅ 加载后画布显示之前创建的所有节点
✅ 节点配置保持不变
✅ 执行路径保持不变

### 实际结果
[ ] 待测试

---

## 测试场景 7: 多页面流程测试

### 测试步骤
1. 创建新的流程图
2. 添加开始节点 → 页面节点A → 页面节点B → 结束节点
3. 为页面A加载快照并配置执行路径
4. 为页面B加载快照并配置执行路径
5. 生成脚本
6. 验证脚本包含两个页面的操作

### 预期结果
✅ 多个页面节点正常连接
✅ 每个页面节点可以独立配置快照
✅ 生成的脚本按拓扑排序顺序执行各页面操作
✅ 脚本中包含所有页面的元素操作

### 实际结果
[ ] 待测试

---

## 测试场景 8: 错误处理测试

### 测试步骤

#### 8.1 未加载快照就生成脚本
1. 创建流程: 开始 → 页面(未加载快照) → 结束
2. 点击"生成脚本"

**预期结果**: ✅ 脚本正常生成，但页面节点部分只有注释，没有元素操作

#### 8.2 空流程图生成脚本
1. 清空画布
2. 点击"生成脚本"

**预期结果**: ✅ 显示警告消息"流程图为空，请先添加节点"

#### 8.3 循环依赖检测
1. 创建流程: 页面A → 页面B → 页面A（形成循环）
2. 点击"生成脚本"

**预期结果**: ✅ 拓扑排序检测到循环，在控制台输出警告，使用原始顺序生成脚本

#### 8.4 无效快照文件
1. 配置页面节点选择不存在的快照文件
2. 点击"加载快照"

**预期结果**: ✅ 显示错误消息"快照文件不存在"或"快照加载失败"

### 实际结果
[ ] 待测试

---

## 性能测试

### 测试项
1. **大型快照加载**: 测试包含100+元素的快照文件加载性能
2. **复杂流程图**: 测试包含20+节点的流程图渲染性能
3. **脚本生成速度**: 测试复杂流程图的脚本生成耗时

### 预期结果
✅ 快照加载时间 < 3秒
✅ 流程图渲染流畅，无明显卡顿
✅ 脚本生成时间 < 1秒

### 实际结果
[ ] 待测试

---

## 兼容性测试

### 测试浏览器
- [ ] Chrome/Edge (Chromium)
- [ ] Firefox
- [ ] Safari (macOS)

### 预期结果
✅ 所有功能在主流浏览器中正常工作
✅ UI显示一致

---

## 已知问题

### 问题列表
1. [ ] 待测试后记录

---

## 测试总结

### 功能完整性
- [ ] 功能1: 页面节点内部元素渲染 - **待测试**
- [ ] 功能2: 元素间连线和执行路径 - **待测试**
- [ ] 功能3: Playwright脚本生成 - **待测试**
- [ ] 功能4: 测试脚本执行 - **待测试**

### 测试结论
**待完成实际测试后填写**

### 建议改进项
**待完成实际测试后填写**

---

## 测试执行人
Claude Code

## 测试执行时间
2026-04-14
