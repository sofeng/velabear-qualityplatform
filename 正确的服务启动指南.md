# 正确的服务启动指南

## ✅ 正确的端口配置

根据 `frontend/vite.config.js` 配置：
- **前端开发服务器**: http://localhost:3000 ✓
- **后端Django服务**: http://localhost:8000 ✓
- **API代理**: 前端3000端口会自动代理 `/api/` 和 `/media/` 到后端8000端口

---

## 🚀 正确的启动步骤

### 方式一：开发环境（推荐）

需要打开 **3个命令行窗口**：

#### 终端1 - Django后端服务
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
python manage.py runserver
```
✅ 启动后访问: http://localhost:8000

#### 终端2 - Celery Worker（已在运行）
```bash
# 当前已有Celery进程在运行，PID: 27928, 24556
# 如需重启：
taskkill /F /IM celery.exe
cd D:\AI\syswin-testhub\testhub-platform-src
celery -A backend worker -l info
```

#### 终端3 - 前端开发服务器
```bash
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev
```
✅ 启动后访问: **http://localhost:3000** ⬅️ 正确的前端地址

---

### 方式二：生产环境

只使用Django静态文件服务（前端已构建到 `frontend/dist/`）:

```bash
# 启动Django
cd D:\AI\syswin-testhub\testhub-platform-src
python manage.py runserver

# Celery保持运行
# 已有进程运行中
```

访问: http://localhost:8000

---

## 📊 访问地址总结

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端开发服务器 | **http://localhost:3000** | 开发环境（热重载） |
| Django后端 | http://localhost:8000 | 后端API服务 |
| Django静态服务 | http://localhost:8000 | 生产环境（使用构建后的前端） |
| API文档 | http://localhost:8000/api/docs/ | Swagger文档 |
| ReDoc文档 | http://localhost:8000/api/redoc/ | ReDoc文档 |

---

## 🔍 验证服务运行

### 1. 检查Django（8000端口）
```bash
# 检查端口
netstat -ano | findstr :8000

# 访问API
curl http://localhost:8000/api/
```

### 2. 检查前端（3000端口）
```bash
# 检查端口
netstat -ano | findstr :3000

# 浏览器访问
http://localhost:3000
```

### 3. 检查Celery
```bash
tasklist | findstr celery
```

---

## ✅ 正确的测试流程

### 第一步：启动Django
```bash
cd D:\AI\syswin-testhub\testhub-platform-src
python manage.py runserver
```

等待看到：
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

### 第二步：启动前端
```bash
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev
```

等待看到：
```
  VITE v4.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://0.0.0.0:3000/
  ➜  press h to show help
```

### 第三步：访问系统
打开浏览器访问: **http://localhost:3000**

### 第四步：测试功能
1. 登录系统
2. 导航到: **AI开发 -> AI开发任务**
3. 测试功能：
   - 查看任务列表
   - 点击"详情"查看任务详情
   - 点击"日志"查看日志
   - 创建新任务验证新功能

---

## 🎯 新功能验证清单

创建新的AI开发任务后，检查详情页面是否显示：

- [ ] **Git仓库地址** - 可点击的链接
- [ ] **开发分支** - 分支名称
- [ ] **Commit Hash** - 可复制
- [ ] **Commit Message** - 完整消息
- [ ] **修改的文件列表** - 以标签形式显示（如：`src/App.vue`）
- [ ] **服务URL** - 如果配置了启动命令

---

## ⚠️ 常见错误修正

### ❌ 错误的访问地址
- ~~http://localhost:5173~~ ❌
- ~~http://localhost:5174~~ ❌

### ✅ 正确的访问地址
- **http://localhost:3000** ✓ (开发环境前端)
- **http://localhost:8000** ✓ (后端API或生产环境)

---

## 📱 移动端/网络访问

如果需要在其他设备访问（同一局域网）:

1. 查看本机IP:
```bash
ipconfig
# 找到IPv4地址，如: 192.168.1.100
```

2. 访问地址:
- 前端: http://192.168.1.100:3000
- 后端: http://192.168.1.100:8000

---

## 🔧 故障排除

### 问题1: 前端启动后显示5173端口

**原因**: Vite配置未生效

**解决**:
```bash
cd frontend
# 检查vite.config.js中的port配置
# 应该显示 port: 3000

# 重启前端服务
npm run dev
```

### 问题2: 端口已被占用

**3000端口被占用**:
```bash
# 查找占用进程
netstat -ano | findstr :3000

# 结束进程
taskkill /PID <进程ID> /F
```

**8000端口被占用**:
```bash
# 查找占用进程
netstat -ano | findstr :8000

# 结束进程
taskkill /PID <进程ID> /F
```

### 问题3: 前端无法连接后端

**检查**:
1. Django是否在8000端口运行
2. vite.config.js中的proxy配置是否正确（应该指向8000端口）
3. 浏览器控制台是否有CORS错误

---

## 🎉 快速开始

**一键启动脚本** (创建 `start_dev.bat`):

```batch
@echo off
echo Starting AIDevTestOps Development Environment...
echo.

echo [1/3] Starting Django Backend...
start cmd /k "cd /d D:\AI\syswin-testhub\testhub-platform-src && python manage.py runserver"

timeout /t 3

echo [2/3] Starting Frontend...
start cmd /k "cd /d D:\AI\syswin-testhub\testhub-platform-src\frontend && npm run dev"

echo [3/3] Opening Browser...
timeout /t 5
start http://localhost:3000

echo.
echo All services started!
echo Frontend: http://localhost:3000
echo Backend:  http://localhost:8000
echo.
pause
```

保存后双击运行即可自动启动所有服务！

---

**现在请使用正确的端口 http://localhost:3000 访问前端！** 🚀
