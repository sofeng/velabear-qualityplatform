# TestHub AI自动开发平台 - 启动服务指南

## 📅 更新时间
2026-04-02

---

## 🚀 快速启动（推荐）

### Windows环境

#### 方法1: 使用PowerShell（3个窗口）

**窗口1: Django后端服务器**
```powershell
# 进入项目目录
cd D:\AI\syswin-testhub\testhub-platform-src

# 激活虚拟环境（如果有）
E:\python_venv\testhub\Scripts\activate.bat

# 启动Django开发服务器
python manage.py runserver
```

**窗口2: 前端开发服务器**
```powershell
# 进入前端目录
cd D:\AI\syswin-testhub\testhub-platform-src\frontend

# 启动Vite开发服务器
npm run dev
```

**窗口3: Redis + Celery Worker（可选，用于异步任务执行）**

**步骤1: 启动Redis**
```powershell
# 下载Redis for Windows后，在Redis目录运行
redis-server.exe

# 或使用Memurai (推荐Windows用户)
# 下载: https://www.memurai.com/
# 安装后会作为Windows服务自动运行
```

**步骤2: 启动Celery Worker**
```powershell
# 进入项目目录
cd D:\AI\syswin-testhub\testhub-platform-src

# 激活虚拟环境
E:\python_venv\testhub\Scripts\activate.bat

# 启动Celery Worker
celery -A backend worker -l info -P solo
```

**预期输出**:
```
-------------- celery@DESKTOP v5.3.4
[tasks]
  . apps.ai_development.tasks.execute_ai_coding
```

> **注意**:
> - ✅ Celery已配置完成
> - ⚠️ 需要Redis运行才能使用Celery
> - 💡 如果不使用Celery，AI开发功能可以同步执行（适合测试）

---

## ✅ 验证服务是否正常运行

### 1. 检查Django后端
在浏览器中访问以下地址：
- http://localhost:8000/ - 应该看到Django欢迎页面或重定向
- http://localhost:8000/api/ - API根路径
- http://localhost:8000/api/docs/ - Swagger API文档
- http://localhost:8000/admin/ - Django管理后台

**预期Django控制台输出**:
```
Performing system checks...

System check identified no issues (0 silenced).
April 02, 2026 - 14:01:00
Django version 4.2.7, using settings 'backend.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

### 2. 检查前端
在浏览器中访问：
- http://localhost:5173/ - 前端首页（Vite默认端口）

**预期前端控制台输出**:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### 3. 测试登录
1. 访问 http://localhost:5173/
2. 使用测试账号登录
3. 登录成功后应该能看到主界面

---

## 🔧 当前运行状态

### ✅ 已启动的服务

| 服务 | 状态 | 地址 | 说明 |
|------|------|------|------|
| Django后端 | ✅ 运行中 | http://127.0.0.1:8000 | 后台任务 b07756a |
| 前端开发服务器 | ✅ 运行中 | http://localhost:5173 | 用户控制终端 |
| Celery Worker | ✅ 已配置 | - | 等待Redis连接（后台任务 b9e38bd） |
| Redis | ❌ 未安装 | redis://127.0.0.1:6379 | 可选，用于Celery消息队列 |

### 查看后台任务日志

**Django日志**:
```bash
# 查看实时日志
type C:\Users\fengs\AppData\Local\Temp\claude\D--AI-syswin-testhub-testhub-platform-src\tasks\b07756a.output

# 或使用PowerShell持续监控
Get-Content C:\Users\fengs\AppData\Local\Temp\claude\D--AI-syswin-testhub-testhub-platform-src\tasks\b07756a.output -Wait
```

---

## 🎯 访问AI开发功能

服务启动成功后，你可以访问以下页面：

1. **AI开发配置管理**
   - 地址: http://localhost:5173/ai-generation/ai-dev-configs
   - 功能: 创建和管理AI开发配置

2. **AI开发任务监控**
   - 地址: http://localhost:5173/ai-generation/ai-dev-tasks
   - 功能: 查看和监控AI开发任务执行状态

3. **需求详情页（创建任务）**
   - 地址: 任何需求详情页
   - 功能: 点击"🤖 AI开发"按钮创建任务

---

## 🛑 停止服务

### 停止Django后端（后台任务）

**使用/tasks命令查看**:
在Claude Code中输入 `/tasks` 查看所有后台任务

**停止特定任务**:
```bash
# 方法1: 通过任务ID停止
# 在Claude Code中找到Django任务ID，然后停止

# 方法2: 查找并终止进程
netstat -ano | findstr :8000
taskkill /F /PID <进程ID>
```

### 停止前端开发服务器

在前端运行的终端窗口中按 `Ctrl+C`

### 停止Celery Worker（如果启动了）

在Celery运行的终端窗口中按 `Ctrl+C`

---

## 🐛 常见问题排查

### 问题1: 端口被占用

**症状**:
```
Error: [Errno 10048] error while attempting to bind on address ('127.0.0.1', 8000)
```

**解决方法**:
```bash
# 查找占用8000端口的进程
netstat -ano | findstr :8000

# 终止进程
taskkill /F /PID <进程ID>

# 或者使用其他端口启动
python manage.py runserver 8001
```

### 问题2: 数据库错误

**症状**:
```
django.db.utils.OperationalError: no such table: xxx
```

**解决方法**:
```bash
# 执行数据库迁移
python manage.py migrate

# 如果需要，重新创建迁移
python manage.py makemigrations
python manage.py migrate
```

### 问题3: 前端无法连接后端

**症状**:
```
Error: connect ECONNREFUSED 127.0.0.1:8000
```

**检查清单**:
- [ ] Django服务器是否正在运行？
- [ ] 访问 http://localhost:8000/api/ 是否正常？
- [ ] 检查前端代理配置（`frontend/vite.config.js`）

**解决方法**:
确保Django服务器正在运行，然后刷新前端页面

### 问题4: 虚拟环境未激活

**症状**:
```
ModuleNotFoundError: No module named 'django'
```

**解决方法**:
```bash
# Windows
E:\python_venv\testhub\Scripts\activate.bat

# 验证
where python
pip list | findstr Django
```

### 问题5: npm依赖缺失

**症状**:
```
Error: Cannot find module 'xxx'
```

**解决方法**:
```bash
cd frontend
npm install
```

---

## 🔧 Redis安装指南（可选）

### 为什么需要Redis？

Redis用于Celery的消息队列，支持：
- ✅ 异步执行AI开发任务
- ✅ 任务队列管理
- ✅ 任务结果存储
- ✅ 分布式任务处理

### Windows安装方案

#### 方案1: Memurai（推荐）

**Memurai** 是Redis的Windows原生版本，完全兼容Redis。

1. **下载安装**:
   - 访问: https://www.memurai.com/
   - 下载免费的Developer版本
   - 运行安装程序

2. **验证安装**:
```powershell
# Memurai会作为Windows服务自动启动
# 测试连接
memurai-cli ping
# 应该返回: PONG
```

#### 方案2: Redis for Windows

1. **下载**:
   - GitHub: https://github.com/tporadowski/redis/releases
   - 下载最新的 `.zip` 或 `.msi` 文件

2. **运行**:
```powershell
# 解压后，在Redis目录运行
redis-server.exe

# 或安装为服务
redis-server.exe --service-install
redis-server.exe --service-start
```

3. **验证**:
```powershell
redis-cli ping
# 应该返回: PONG
```

#### 方案3: Docker方式

```bash
# 使用Docker运行Redis
docker run -d -p 6379:6379 --name redis redis:latest

# 验证
docker exec -it redis redis-cli ping
```

### 配置说明

Redis默认配置已在 `backend/settings.py` 中：
```python
CELERY_BROKER_URL = 'redis://:1234@127.0.0.1:6379/0'
CELERY_RESULT_BACKEND = 'redis://:1234@127.0.0.1:6379/0'
```

如果Redis没有密码，修改为：
```python
CELERY_BROKER_URL = 'redis://127.0.0.1:6379/0'
CELERY_RESULT_BACKEND = 'redis://127.0.0.1:6379/0'
```

---

## 📦 依赖检查

### 后端依赖

```bash
# 检查Python版本（推荐3.10+）
python --version

# 检查Django版本
python -c "import django; print(django.get_version())"

# 检查关键包
pip list | findstr "django anthropic openai docker"
```

**必需的包**:
- django >= 4.2
- djangorestframework
- anthropic (Anthropic API)
- openai (DeepSeek API)
- docker (Docker集成)
- celery (异步任务，可选)

### 前端依赖

```bash
cd frontend

# 检查Node版本（推荐16+）
node --version

# 检查npm版本
npm --version

# 检查关键包
npm list vue element-plus axios
```

---

## 🔄 重启服务流程

当修改了代码或配置后：

### 后端修改后
1. 停止Django服务器（Ctrl+C或停止后台任务）
2. 如果修改了models.py，运行迁移：
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```
3. 重新启动Django服务器

### 前端修改后
- Vite会自动热重载，无需重启
- 如果修改了配置文件（vite.config.js），需要重启

### 依赖更新后
```bash
# 后端
pip install -r requirements.txt

# 前端
cd frontend
npm install
```

---

## 🧪 测试服务连通性

### 快速测试脚本

**测试后端API**:
```bash
# 测试根路径
curl http://localhost:8000/api/

# 测试AI开发配置API
curl http://localhost:8000/api/ai-development/configs/
```

**测试前端**:
```bash
# 在浏览器中打开
start http://localhost:5173
```

---

## 📊 性能监控

### Django请求日志
Django控制台会实时显示每个API请求：
```
[02/Apr/2026 14:01:10] "GET /api/projects/ HTTP/1.1" 200 1234
[02/Apr/2026 14:01:15] "POST /api/ai-development/tasks/create_task/ HTTP/1.1" 201 567
```

### 前端性能
在浏览器开发者工具中查看：
- Network标签：API请求耗时
- Console标签：前端错误和警告

---

## 🎯 AI开发功能专项检查

### 必需服务
- [x] Django后端 (http://localhost:8000)
- [x] 前端开发服务器 (http://localhost:5173)
- [ ] Celery Worker (可选，用于真正的异步执行)

### DeepSeek API验证
```bash
# 设置API Key
$env:DEEPSEEK_API_KEY="sk-your-key-here"

# 运行验证脚本
python tests/verify_deepseek_api.py
```

### Docker检查
```bash
# 检查Docker是否运行
docker ps

# 检查Docker版本
docker --version
```

---

## 📝 启动检查清单

使用此清单确保所有服务正常运行：

### 启动前检查
- [ ] Python虚拟环境已激活
- [ ] Node.js和npm已安装
- [ ] Docker Desktop正在运行（如果使用Docker）
- [ ] 数据库迁移已完成
- [ ] 端口8000和5173未被占用

### 启动服务
- [ ] Django后端已启动（http://localhost:8000）
- [ ] 前端开发服务器已启动（http://localhost:5173）
- [ ] Celery Worker已启动（可选）

### 功能验证
- [ ] 可以访问Django Admin (http://localhost:8000/admin/)
- [ ] 可以访问API文档 (http://localhost:8000/api/docs/)
- [ ] 可以访问前端首页 (http://localhost:5173/)
- [ ] 可以登录系统
- [ ] 可以访问AI开发配置页面
- [ ] 可以访问AI开发任务页面

---

## 🚀 生产环境部署

（待完善）

生产环境需要额外配置：
- [ ] 使用Gunicorn/uWSGI运行Django
- [ ] 使用Nginx作为反向代理
- [ ] 配置Redis作为Celery Broker
- [ ] 配置PostgreSQL/MySQL数据库
- [ ] 设置环境变量和密钥
- [ ] 配置HTTPS
- [ ] 设置日志收集
- [ ] 配置监控告警

---

## 📞 支持与帮助

### 查看日志
- Django日志: 终端输出或日志文件
- 前端日志: 浏览器Console
- Celery日志: 终端输出

### 获取帮助
- 项目文档: `README.md`
- API文档: http://localhost:8000/api/docs/
- 技术方案: `AI自动编码功能技术方案.md`
- 前端完成报告: `AI开发功能前端完成报告.md`

---

**文档版本**: v1.0
**更新时间**: 2026-04-02
**适用环境**: Windows开发环境

---

## 🎉 快速启动命令总结

```powershell
# 终端1: Django后端
cd D:\AI\syswin-testhub\testhub-platform-src
E:\python_venv\testhub\Scripts\activate.bat
python manage.py runserver

# 终端2: 前端
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev

# 终端3: Celery (可选)
cd D:\AI\syswin-testhub\testhub-platform-src
E:\python_venv\testhub\Scripts\activate.bat
celery -A backend worker -l info -P solo
```

**就这么简单！** 🚀
