# 前端访问问题排查指南

## ✅ 服务状态确认

刚才的检查结果显示：
- **3000端口**: ✅ 正在监听（PID: 32916）
- **HTTP响应**: ✅ 返回 200 OK
- **Node进程**: ✅ 正在运行（PID: 30672）

**结论**: 前端服务已经正常启动！问题在于浏览器访问。

---

## 🔧 解决方案（按顺序尝试）

### 方案1: 清除浏览器缓存并强制刷新 ⭐

1. 打开浏览器访问: http://localhost:3000
2. 按下快捷键: **Ctrl + Shift + Delete**
3. 勾选"缓存的图片和文件"
4. 点击"清除数据"
5. 按 **Ctrl + F5** 强制刷新页面

### 方案2: 使用不同的浏览器

如果您用的是Chrome，尝试：
- Microsoft Edge
- Firefox
- 或Chrome的隐私模式（Ctrl + Shift + N）

访问: http://localhost:3000

### 方案3: 确认URL格式正确

确保访问的是以下任一地址：
- ✅ http://localhost:3000
- ✅ http://localhost:3000/
- ✅ http://127.0.0.1:3000
- ✅ http://127.0.0.1:3000/

**注意**: 不要加 https:// 前缀

### 方案4: 检查浏览器控制台

1. 打开浏览器开发者工具（F12）
2. 切换到"控制台"(Console)标签
3. 刷新页面
4. 查看是否有错误信息
5. 切换到"网络"(Network)标签
6. 刷新页面，查看请求状态

### 方案5: 测试网络连接

在浏览器地址栏输入：
```
http://localhost:3000/index.html
```

或者用curl测试（命令行）:
```bash
curl http://localhost:3000
```

### 方案6: 检查防火墙/杀毒软件

某些安全软件可能阻止localhost访问：
1. 临时关闭防火墙测试
2. 检查杀毒软件设置
3. 将localhost加入白名单

### 方案7: 使用IP地址访问

如果localhost不工作，尝试用IP：
```
http://127.0.0.1:3000
```

或查看本机IP后访问：
```bash
ipconfig
# 找到IPv4地址，如: 192.168.1.100
# 然后访问: http://192.168.1.100:3000
```

---

## 📊 诊断信息收集

如果以上方法都不行，请提供以下信息：

### 1. 浏览器信息
- 浏览器名称和版本
- 是否能访问其他localhost服务（如 http://localhost:8000）

### 2. 网络测试结果
在命令行运行：
```bash
# 测试连接
curl http://localhost:3000

# 查看详细响应
curl -v http://localhost:3000
```

### 3. 浏览器开发者工具
- F12打开开发者工具
- 截图"控制台"标签的错误
- 截图"网络"标签的请求列表

### 4. 防火墙状态
```bash
netsh advfirewall show allprofiles state
```

---

## 🚀 快速测试脚本

我创建了一个测试脚本，运行它查看详细信息：

**test_frontend.bat** (已创建在项目根目录):
```batch
@echo off
echo ====================================
echo 前端服务诊断工具
echo ====================================
echo.

echo [1] 检查3000端口...
netstat -ano | findstr :3000
echo.

echo [2] 检查Node进程...
tasklist | findstr node
echo.

echo [3] 测试HTTP连接...
curl -I http://localhost:3000
echo.

echo [4] 测试127.0.0.1连接...
curl -I http://127.0.0.1:3000
echo.

echo [5] 查看本机IP地址...
ipconfig | findstr IPv4
echo.

echo ====================================
echo 诊断完成
echo ====================================
echo.
echo 请尝试在浏览器访问以下地址:
echo   - http://localhost:3000
echo   - http://127.0.0.1:3000
echo   - http://本机IP:3000
echo.
pause
```

---

## ✅ 验证服务正常的标志

当成功访问时，您应该看到：
1. **AIDevTestOps登录页面**
2. 页面标题包含"AIDevTestOps"
3. 有用户名和密码输入框
4. 有"登录"按钮

---

## 🔍 常见错误和解决

### 错误1: "无法访问此网站"
**原因**: 服务未启动或端口错误
**解决**:
```bash
# 重启前端服务
cd D:\AI\syswin-testhub\testhub-platform-src\frontend
npm run dev
```

### 错误2: "ERR_CONNECTION_REFUSED"
**原因**: 端口未监听
**解决**: 检查是否有其他程序占用3000端口
```bash
netstat -ano | findstr :3000
```

### 错误3: 页面空白
**原因**: 构建问题或缓存问题
**解决**:
```bash
# 清除缓存重新构建
cd frontend
rm -rf node_modules/.vite
npm run dev
```

### 错误4: "localhost拒绝了我们的连接请求"
**原因**: 防火墙或hosts文件配置问题
**解决**:
1. 检查 C:\Windows\System32\drivers\etc\hosts 文件
2. 确认有这一行: `127.0.0.1 localhost`

---

## 📞 紧急备用方案

如果所有方法都失败，使用生产模式访问：

1. 停止前端开发服务器
2. 确保Django在运行
3. 访问: **http://localhost:8000**

Django会服务已构建的前端文件（因为我们已经运行了 `npm run build`）。

---

**现在请尝试方案1（清除缓存并强制刷新），这通常能解决大多数问题！** 🚀
